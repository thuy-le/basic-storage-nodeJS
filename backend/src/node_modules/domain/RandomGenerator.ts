export function MakeRandomGenerator() {
    return new RandomGenerator();
}

export interface IRandomGenerator {
    generateChars(): string;
}

export function MakeRandomChars() {
    return new RandomGenerator().generateChars();
}

export class RandomGenerator implements IRandomGenerator {

    public generateChars(): string {
        return Math.random().toString(36).substring(5);
    }

}

