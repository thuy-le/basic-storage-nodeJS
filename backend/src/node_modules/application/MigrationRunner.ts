const WaitFor = require("framework/WaitFor");
const wait = require("wait.for");
const winston = require("winston");
const Promise = require('promise');
import {up as UserDataUp} from "migrations/UserData";

export default function MigrationRunner(result) {
    wait.launchFiber(() => {
        console.log(result, "!!!!");
        UserDataUp();
    });    
}
