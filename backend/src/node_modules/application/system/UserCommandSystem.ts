import {CreateUserData} from "domain/user/User";
import {MakeUserGateway, UserGateway} from "gateway/user/UserGateway";
import {InvalidArgumentException} from "domain/Exception";


export function MakeUserCommandSystem(gateway?: UserGateway) {
    return new UserCommandSystem(gateway || MakeUserGateway());
}


class UserCommandSystem {
    private gateway: UserGateway;

    constructor(gateway: UserGateway) {
        this.gateway = gateway;
    }

    public createUser(data: CreateUserData) {
        const newUser = this.gateway.create(data);
        return newUser;    
    }
}
